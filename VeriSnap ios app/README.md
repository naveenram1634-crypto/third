# VeriSnap iOS App (MVP Scaffold)

Starter scaffold based on `truthguard_mvp_blueprint.md`.

## Included
- Flutter-ready project structure
- Bottom tabs: Home, Scan, Education, History
- MVP starter UI in `lib/main.dart`

## Next
1. Install Flutter SDK (not available in current environment).
2. From this folder run:
   - `flutter pub get`
   - `flutter run`

## Path
`/Users/navi/my-react-app/VeriSnap ios app`
